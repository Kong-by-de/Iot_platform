# IoT Platform

## Что это за проект

IoT Platform — это связка сервисов, которая позволяет:

- подключать свои устройства и хранить их телеметрию;
- задавать **правила срабатывания алертов** (по температуре, влажности);
- получать уведомления **в Telegram-бот** и **на e-mail**, когда условия нарушаются;
- работать как с **конкретным устройством**, так и сразу со **всеми авторизованными устройствами пользователя**.

---

## Основной сценарий для пользователя

1. **Пользователь заходит в Telegram-бота.**  
   Там он:
   - привязывает свой аккаунт к платформе;
   - авторизует одно или несколько устройств (по токену/ID).

2. **Настраивает правила (алерты):**
   - задаёт пороги метрик валжности и температуры: «выше X», «ниже Y»;
   - указывает область действия:
     - либо для **конкретного устройства**,
     - либо сразу для **всех своих устройств**.

3. **Устройства шлют телеметрию** в платформу:  
   данные собирает Java-сервис , записывает в БД.
   а HTTP-сервер С++ обращается к БД для проверки на срабатывание правил.

4. **Rule Engine + Alert Service** анализируют новые значения:
   - если одно из правил нарушено — формируется событие алерта;
   - применяется анти-спам логика, чтобы не засыпать пользователя одинаковыми уведомлениями.

5. **NotificationService** отправляет уведомление:
   - в Telegram (через Bot API);
   - на e-mail (красивое HTML-письмо со всей информацией по устройству и метрике).

---

## Ключевые возможности

- 🔐 **Привязка устройств к пользователю**
  - авторизация устройств через Telegram-бота;
  - хранение списка устройств и подписок в PostgreSQL.

- 📏 **Гибкие правила алертов**
  - отдельные пороги для температуры и влажности;
  - правила «выше порога / ниже порога»;
  - действие правила:
    - на одно устройство;
    - на все устройства пользователя.

- 🔔 **Уведомления в два канала**
  - Telegram-сообщения с форматированным текстом;
  - HTML-e-mail с таблицей параметров, эмодзи-индикаторами и рекомендациями.



---

## Архитектура (кратко)

- 🧱 **Модульная архитектура**
  - `core/` — ядро (конфигурация, база данных, уведомления);
  - `services/` — бизнес-логика оповещений и статистики;
  - `api/` — HTTP-сервер и API для доступа к данным;
  - `bot/` — обработчик Telegram-бота;
  - `smtp/` — отправка почты через SMTP (libcurl);
  - `simulation/` — симулятор устройств для отладки.


- **Java-сервис**  
  Получает данные от реальных устройств и пишет их в БД.

- **C++ HTTP-сервер (`src/api`)**  
  Предоставляет API:
  - для получения телеметрии;
  - для управления правилами и устройствами;
  - для интеграции с ботом.

- **PostgreSQL + libpqxx**  
  Хранение:
  - устройств;
  - пользователей и их подписок;
  - истории телеметрии;
  - правил алертов.

- **AlertProcessingService (`src/services/AlertService.cpp`)**
  - периодически опрашивает актуальные данные по устройствах;
  - применяет пользовательские правила;
  - инициирует отправку уведомлений.

- **NotificationService (`src/core/NotificationService.cpp`)**
  - инкапсулирует отправку в Telegram;
  - интеграция с EmailService для отправки писем.

- **EmailService (`src/smtp/EmailService.cpp`)**
  - отправка через SMTP (libcurl);
  - формирование красивого HTML-письма с данными устройства и рекомендациями.

---

## Технологический стек

- C++17, CMake  
- PostgreSQL + libpqxx  
- libcurl (SMTP), cpr (HTTP-клиент), OpenSSL  
- Telegram Bot API  
- GoogleTest (юнит-тесты)  
- Опционально `yaml-cpp` для YAML-конфига  

